import 'package:flutter/material.dart';

class EntryFragment extends StatefulWidget {
  const EntryFragment({super.key});

  @override
  State<EntryFragment> createState() => _EntryFragmentState();
}

class _EntryFragmentState extends State<EntryFragment> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Material(
      child: Stack(
        children: [

        ],
      ),
    ));
  }
}
